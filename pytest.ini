[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    contract: contract tests for API behavior
    integration: integration tests against the application stack
    unit: unit tests for domain/service logic
